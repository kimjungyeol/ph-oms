<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ktnet.fta.common.mapper.CommonCodeMapper">

    <select id="selectCodeOne" parameterType="hashmap" resultType="camelMap">
        /* ProgramID=commonCodeMapper.selectCodeOne */
        SELECT  CMMN_CD AS CODE
               ,CMMN_NM AS VALUE
          FROM COMMON_CODE
         WHERE CMMN_GRP_CD = UPPER( #{grpCd} )
           AND CMMN_CD = UPPER( #{cmmnCd} )
           AND USE_FLAG = 'Y'
    </select>

</mapper>